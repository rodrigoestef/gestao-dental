import { TextField } from "@material-ui/core";
import styled from "styled-components";

export const CustonTextField = styled(TextField).attrs({
  size: "small",
  fullWidth: true,
  variant: "outlined",
})``;
